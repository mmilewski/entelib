{{ user.first_name }} {{ user.last_name }} has requested access to library.

Activate him at {{ application_url }}{% url activate_user user.id %}.

See his profile at {{ application_url }}{% url user_one user.id %}.
